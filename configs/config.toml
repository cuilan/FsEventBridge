[server]
socket_path = "/tmp/feb.sock"
log_level = "info"

[monitor]
path = "/data/sate"
recursive = true
events = ["CLOSE_WRITE", "MOVED_TO"]
exclude_extensions = [".tmp", ".swp"]

[processor]
# 启用 io_uring 加速
use_io_uring = true
worker_threads = 4