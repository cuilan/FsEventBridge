[server]
socket_path = "/tmp/feb.sock"
log_level = "info"

[monitor]
path = "/tmp"
recursive = true
events = ["CLOSE_WRITE", "MOVED_TO"]
exclude_extensions = [".tmp", ".swp"]
exclude_path = ["", ""]

[processor]
# 启用 io_uring 加速
use_io_uring = true
worker_threads = 4